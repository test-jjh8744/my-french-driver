import {
  __commonJS
} from "./chunk-V4OQ3NZ2.js";

// node_modules/haversine-distance/index.js
var require_haversine_distance = __commonJS({
  "node_modules/haversine-distance/index.js"(exports, module) {
    var atan2 = Math.atan2;
    var cos = Math.cos;
    var sin = Math.sin;
    var sqrt = Math.sqrt;
    var PI = Math.PI;
    var R = 6378137;
    function squared(x) {
      return x * x;
    }
    function toRad(x) {
      return x * PI / 180;
    }
    function hav(x) {
      return squared(sin(x / 2));
    }
    function haversineDistance(a, b) {
      const aLat = toRad(Array.isArray(a) ? a[1] : a.latitude ?? a.lat);
      const bLat = toRad(Array.isArray(b) ? b[1] : b.latitude ?? b.lat);
      const aLng = toRad(Array.isArray(a) ? a[0] : a.longitude ?? a.lng ?? a.lon);
      const bLng = toRad(Array.isArray(b) ? b[0] : b.longitude ?? b.lng ?? b.lon);
      const ht = hav(bLat - aLat) + cos(aLat) * cos(bLat) * hav(bLng - aLng);
      return 2 * R * atan2(sqrt(ht), sqrt(1 - ht));
    }
    module.exports = haversineDistance;
  }
});
export default require_haversine_distance();
//# sourceMappingURL=haversine-distance.js.map
